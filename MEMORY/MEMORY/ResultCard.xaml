<UserControl x:Class="MEMORY.ResultCard"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:local="clr-namespace:MEMORY"
			 mc:Ignorable="d" 
			 d:DesignHeight="100" d:DesignWidth="800">
	<Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock FontSize="40" Style="{DynamicResource TottalTbStyle}" Grid.Column="0" x:Name="NumTb" Margin="10"/>
        <TextBlock FontSize="40" Style="{DynamicResource TottalTbStyle}" Grid.Column="1" x:Name="TimeTb" Margin="10"/>
        <TextBlock FontSize="40" Style="{DynamicResource TottalTbStyle}" Grid.Column="2" x:Name="DifficultTb" HorizontalAlignment="Right" Margin="10"/>

    </Grid>
</UserControl>
