<UserControl x:Class="MEMORY.Authors"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MEMORY"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      FontFamily="/MEMORY;component/Fonts/#Baymax">

    <Border CornerRadius="20" Background="{DynamicResource PagesBackground}">
        <Grid Margin="50">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            
            <TextBlock HorizontalAlignment="Right" Text="Глава разработки:" FontSize="50" Margin="20" Grid.Row="0" Style="{DynamicResource TottalTbStyle}"/>
            <TextBlock HorizontalAlignment="Right" Text="Дизайнер:" FontSize="50" Margin="20" Grid.Row="1" Style="{DynamicResource TottalTbStyle}"/>
            <TextBlock HorizontalAlignment="Right" Text="Разработчики:" FontSize="50" Margin="20" Grid.Row="2" Style="{DynamicResource TottalTbStyle}"/>

            <TextBlock Grid.Column="1" Text="Ваккер Артур" FontSize="50" Margin="20" Grid.Row="0" Style="{DynamicResource TottalTbStyle}"/>
            <TextBlock Grid.Column="1" Text="Вагин Кирилл" FontSize="50" Margin="20" Grid.Row="1" Style="{DynamicResource TottalTbStyle}"/>
            <TextBlock Grid.Column="1" Text="Витчинов Ярослав" FontSize="50" Margin="20" Grid.Row="2" Style="{DynamicResource TottalTbStyle}"/>
            <TextBlock Grid.Column="1" Text="Полунин Максим" FontSize="50" Margin="20" Grid.Row="3" Style="{DynamicResource TottalTbStyle}"/>
        </Grid>
    </Border>
</UserControl>
